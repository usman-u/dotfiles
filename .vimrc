" wraps lines
set wrap

" makes tabs 4 spaces
set tabstop=4 softtabstop=4

set shiftwidth=4



set scrolloff=8

" shows line numbers
set number

" allows copying without numbers
se mouse+=a


set smartindent

set ignorecase

set paste
